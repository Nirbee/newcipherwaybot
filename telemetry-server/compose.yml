services:
  telemetry:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8088:8088"
    volumes:
      - ./data:/data
    env_file:
      # copy .env.example → .env first; optional so `up` doesn't hard-fail before you do
      - path: .env
        required: false
